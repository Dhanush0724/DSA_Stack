class Solution:
    def buildArray(self, target: List[int], n: int) -> List[str]:
        stack  = []
        idx = 0
        for i in range(1,n+1):
            stack.append("Push")
            if i == target[idx]:
                idx+=1
                if idx == len(target):
                    return stack
            else :
                stack.append("Pop")
        return stack
            

            

        
